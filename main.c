#include<stdio.h>
#include<conio.h>
void main()
{
  int first,secand,multipal;
    printf("Enter the two no for multiple \n");
    scanf("%d%d",&first,&secand);
    multipal=first*secand;
    printf("the multiple of two no is %d",multipal);
    getch();
}
